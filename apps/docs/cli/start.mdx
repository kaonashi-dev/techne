---
title: "techne start"
description: "Run without hot reload (production-style)."
---

```bash
# Run without hot reload (production-style)
bunx @kaonashi-dev/techne start --port 3000
```

Use `start` in production-style runs and `dev` during local development.
Port resolution is the same as [`bootstrap()`](/configuration/techne-config).
