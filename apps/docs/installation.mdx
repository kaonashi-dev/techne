---
title: "Installation"
description: "Install Techne with bun add and run the CLI without a global install."
---

```bash
bun add @kaonashi-dev/techne
```

To use the CLI without installing it globally:

```bash
bunx @kaonashi-dev/techne --help
```

Once installed, continue with the [Quickstart](/quickstart).
