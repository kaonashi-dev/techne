---
title: "Migrating from Bnest"
description: "Upgrade path from @kaonashi-dev/bnest to @kaonashi-dev/techne (package rename, Redis prefixes, problem `type` URL)."
---

The canonical migration guide is [`MIGRATING.md`](https://github.com/kaonashi-dev/techne/blob/main/MIGRATING.md)
in the repo root. The summary below covers the breaking pieces — read the
full doc before a production upgrade.

## Install the new package

```bash
bun remove @kaonashi-dev/bnest
bun add @kaonashi-dev/techne
```

The legacy `@kaonashi-dev/bnest@0.4.0` is published as a thin shim that
re-exports everything from `@kaonashi-dev/techne` and prints a one-time
deprecation warning.

## Update imports

```diff
-import { BnestFactory } from "@kaonashi-dev/bnest/core";
+import { TechneFactory } from "@kaonashi-dev/techne/core";
```

Renames at a glance:

| Bnest (deprecated)              | Techne (canonical)                 |
| ------------------------------- | ---------------------------------- |
| `BnestFactory`                  | `TechneFactory`                    |
| `BnestApplication`              | `TechneApplication`                |
| `BnestApplicationContext`       | `TechneApplicationContext`         |
| `defineBnestConfig`             | `defineTechneConfig`               |
| `bnest()` shorthand             | `techne()`                         |
| `bootstrap()`                   | `bootstrap()` *(unchanged)*        |

All legacy names are scheduled for removal in **v0.5+**.

## Rename the config file

```bash
mv bnest.config.ts techne.config.ts
```

The framework still loads `bnest.config.ts` if it finds it (and prints a
one-time warning telling you to rename), so this step can wait, but
`techne.config.ts` is the only recognised name in v0.5+.

## Redis prefix migration **(breaking)**

The Redis-backed MQ and queue adapters changed their default key prefixes:

| Adapter | Old default | New default |
| --- | --- | --- |
| MQ | `bnest:mq` | `techne:mq` |
| Queue | `bnest:queue` | `techne:queue` |

**If you have running services with in-flight jobs or messages stored under
the old prefixes, you must rename those keys before upgrading.** See the
full guide for `redis-cli RENAME` recipes, or pin the old prefix
explicitly:

```ts
new Queue("emails", { connection: { prefix: "bnest:queue" } });
new RedisDriver({ connection: { prefix: "bnest:mq" } });
```

## RFC 7807 problem document `type` URL **(breaking for clients)**

```diff
-"type": "https://bnest.dev/errors/not-found"
+"type": "https://github.com/kaonashi-dev/techne/blob/main/docs/errors/not-found.md"
```

If you have client code matching on the old `bnest.dev` URL host, update
the matcher accordingly. The slugs themselves
(`not-found`, `bad-request`, etc.) are unchanged.

## More

For the full upgrade recipe (CLI rename, logger format changes, OpenAPI
title, doctor checks, v1 flat HTTP surface, and a verification
checklist), read [`MIGRATING.md`](https://github.com/kaonashi-dev/techne/blob/main/MIGRATING.md).
